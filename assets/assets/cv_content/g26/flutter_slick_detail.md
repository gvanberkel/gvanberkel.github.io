The main focus areas are:

- An easy to use MVVM framework (ViewModels backing your views).
- Simple routing and menu system (based on Navigator 2.0).
- Permission management.
- Page level lifecycle abstractions.
- User authentication hooks.
- Loading, invalid route and error screen widget hooks (with defaults if not implemented).
- Common responsive screen layout patterns.
